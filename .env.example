# Arquivo de Exemplo para Variáveis de Ambiente
# Copie este arquivo para .env e preencha com seus valores.

# --- Autenticação NextAuth ---
# Uma string secreta para assinar os tokens JWT.
# Gere uma em: https://generate-secret.vercel.app/32
NEXTAUTH_SECRET=

# --- Conexão com o Banco de Dados (MongoDB) ---
# URL de conexão completa para o seu banco de dados MongoDB.
# Exemplo: mongodb+srv://<user>:<password>@<cluster-url>/<db-name>?retryWrites=true&w=majority
MONGODB_URI=
# Nome do banco de dados MongoDB que será utilizado.
MONGODB_DATABASE=
# Nome da collection (tabela) onde os dados do TinaCMS serão armazenados.
MONGODB_COLLECTION=

# --- Autenticação do GitHub (Opcional, para provedor Git) ---
# Se você planeja usar o TinaCMS com um repositório GitHub privado.
# Crie um OAuth App no GitHub para obter essas chaves.
# Documentação: https://tina.io/docs/self-hosted/authentication/#github-oauth-app-setup
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
